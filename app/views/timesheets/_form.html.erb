<%= form_for(@timesheet) do |f| %>
  <% if @timesheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@timesheet.errors.count, "error") %> prohibited this timesheet from being saved:</h2>

      <ul>
      <% @timesheet.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br />
    <%= f.text_field :date, :class => 'datepicker'%>     
  </div>
  
  <div class="field">
  <%= f.fields_for :project_efforts do |builder| %> 
  <%= render 'project_effort', :f => builder %>
  <% end %>
  </div>
  
  <div>	
  <%= link_to_add_fields "Add New Project", f, :project_efforts %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
  
<% end %>
