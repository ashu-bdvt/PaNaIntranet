<h1>Listing leave_types</h1>

<table border = '1'>
  <tr>
    <th>Name</th>
    <th>Max leaves</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @leave_types.each do |leave_type| %>
  <tr>
    <td><%= leave_type.name %></td>
    <td><%= leave_type.max_leaves %></td>
    <td><%= link_to 'Show', leave_type %></td>
    <td><%= link_to 'Edit', edit_leave_type_path(leave_type) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Leave type', new_leave_type_path %><br />
<%= link_to "Back", '/' %>
